{
  "name": "nodejs-memory-game",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/index.js",
    "dev": "nodemon --legacy-watch app --legacy-watch controllers --legacy-watch views --legacy-watch models --legacy-watch plugins --inspect=0.0.0.0:3000 src/index.js",
    "deploy": "now --public && now alias nodejs-memory-game.now.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.17.0",
    "nodemon": "^1.19.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "mongoose": "^5.5.11",
    "socket.io": "^2.2.0"
  }
}
